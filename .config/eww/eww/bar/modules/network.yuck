(deflisten network `./bar/scripts/network.sh`)

(defwidget network [] 
	(box :space-evenly false :orientation "h"
  (eventbox :cursor "pointer" :onclick "./bar/popup-menus/scripts/toggle-wifi.sh" (label :text "${network.icon}" :class "net-i"))
	(label :text "${network.name}" :class "net-t"))
)
